name: "test"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "🍉"
          echo "${{ toJSON(github.event.commits) }}"
          echo "🥝"
